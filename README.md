## rai-compact-saver

Purpose: save tokens with strict minimal replies.

Activation:
- `/startsaving` to enable.
- `/stopsaving` to disable.

Specification lives in `SKILL.md`.
Examples live in `references/examples.md`.
